module.exports = {
  secret: "jiraapplication",
  //url: "mongodb://localhost/jira"
  url: "mongodb://database/mean-docker"
}

// Note: if the local connection fails then
// try using 127.0.0.1 instead of localhost.
